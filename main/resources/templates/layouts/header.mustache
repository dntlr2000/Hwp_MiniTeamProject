<!-- header.mustache (또는 layouts/header.mustache) -->
<!doctype html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <title>나의 감정 일기</title>

    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* ---------- FeelLog 테마 ---------- */
        :root{
          --feel-start:#7c3aed;   /* 왼쪽 그라데이션 색  */
          --feel-end:#f472b6;     /* 오른쪽 그라데이션 색 */
          --feel-bg:#f8f9fa;      /* 기본 배경색 */
        }
        body{ background:var(--feel-bg); }
        .navbar-feel{
          background:linear-gradient(90deg,var(--feel-start),var(--feel-end));
        }
        .navbar-feel .nav-link,
        .navbar-feel .navbar-brand,
        .navbar-feel .form-control,
        .navbar-feel .btn{
          color:#fff;
        }
        .navbar-feel .form-control::placeholder{ color:rgba(255,255,255,.7); }
        .navbar-feel .btn-outline-light{ border-color:#fff; }
        .navbar-feel .btn-outline-light:hover{ background:#fff;color:var(--feel-start); }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-feel shadow-sm py-3">
    <div class="container-xxl">

        <!-- 로고 -->
        <a class="navbar-brand fw-bold d-flex align-items-center" href="/diary">
            <span class="me-2 fs-4">📔</span> 나의 감정일기
        </a>

        <!-- 모바일 토글 -->
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#feelNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- 내비게이션 영역 -->
        <div class="collapse navbar-collapse" id="feelNav">
            <!-- 오른쪽: 검색 + 로그인 -->
            <ul class="navbar-nav ms-auto align-items-lg-center gap-2">
                <!--
                <li class="nav-item">

                    <form action="/diary/search" class="d-flex">
                        <input class="form-control form-control-sm bg-transparent border-0 me-2" type="search"
                               placeholder="제목 / 내용 검색" name="q">
                        <button class="btn btn-outline-light btn-sm">찾기</button>
                    </form>
                </li>
                 -->
                <li class="nav-item">
                    <a class="btn btn-outline-light btn-sm" href="/users/login">로그인</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light btn-sm" href="/users/register">회원가입</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<!-- 헤더 아래부터 본문이 들어갑니다 -->
<div class="container py-4">
