<!-- templates/layouts/header.mustache -->
<!doctype html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>나의 감정일기</title>
    <!-- Bootstrap 5 CSS -->
    <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
            crossorigin="anonymous"
    />
    <style>
        :root {
            --feel-start: #7c3aed;
            --feel-end:   #f472b6;
            --feel-bg:    #f8f9fa;
        }
        body {
            background: var(--feel-bg);
        }
        .navbar-feel {
            background: linear-gradient(90deg, var(--feel-start), var(--feel-end));
        }
        .navbar-feel .nav-link,
        .navbar-feel .navbar-brand,
        .navbar-feel .btn {
            color: #fff;
        }
        .navbar-feel .form-control::placeholder {
            color: rgba(255,255,255,.7);
        }
        .navbar-feel .btn-outline-light {
            border-color: #fff;
        }
        .navbar-feel .btn-outline-light:hover {
            background: #fff;
            color: var(--feel-start);
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-feel shadow-sm py-3">
    <div class="container-xxl">
        <!-- 로고 -->
        <a class="navbar-brand fw-bold d-flex align-items-center" href="/diary">
            <span class="me-2 fs-4">📔</span> 나의 감정일기
        </a>
        <!-- 모바일 토글 -->
        <button
                class="navbar-toggler border-0"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#feelNav"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <!-- 내비게이션 -->
        <div class="collapse navbar-collapse" id="feelNav">
            <ul class="navbar-nav ms-auto align-items-lg-center gap-2">
                <li class="nav-item">
                    <a class="btn btn-outline-light btn-sm" href="/users/login">로그인</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-light btn-sm" href="/users/signup">회원가입</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container py-4">
    <!-- 여기부터 각 페이지 본문이 렌더링 됩니다 -->
